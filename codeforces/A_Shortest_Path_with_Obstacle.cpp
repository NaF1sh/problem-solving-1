#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define cases(n) \
    ll n;        \
    cin >> n;    \
    while (n--)
#define fastread()           \
    ios::sync_with_stdio(0); \
    cin.tie(0);

void solve()
{
    ll aX, ay, bx, by, fx, fy;
    cin >> aX >> ay >> bx >> by;
    cin >> fx >> fy;

    ll countx = 0;
    ll county = 0;

    bool flag = false;
    ll count = 0;
    while (aX < bx)
    {
        count++;
    }
}

int main()
{
    fastread();

    cases(n)
    {
        solve();
    }
}
