#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{

    long long int n;
    cin >> n;
    while (n--)
    {
        long long int a, b;
        cin >> a >> b;
        long long int sum = 0;
        sum = a * b;
        cout << sum << endl;
    }
}